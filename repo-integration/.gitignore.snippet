# Optional: ignore annotation assets (screenshots)
.agent-annotations/assets/

# Optional: ignore receiver token (if you prefer per-dev local tokens)
# .agent-annotations/token.txt

# Optional: ignore open/resolved inbox files (if you don't want notes in git)
# .agent-annotations/inbox.jsonl
# .agent-annotations/inbox-resolved.jsonl
